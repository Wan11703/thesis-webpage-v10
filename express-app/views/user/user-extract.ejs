<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/css/user/user-extract.css" />
  <title>MedExtract</title>
</head>

<body>
  <nav class="user-navbar">
    <div class="container">
      <div class="logo">
        <h1><a href="/user-extract">MedExtract</a></h1>
      </div>

      <div class="menu-toggle" id="user-menu-toggle">&#9776;</div> <!-- Unique ID -->

      <ul class="nav" id="user-nav">
        <li class="username-holder">Hey there! <%= username %>
        </li>
        <li><a href="/user-extract">Extract</a></li>
        <li><a href="/user-search">Search</a></li>
        <li><a href="/user-settings">Settings</a></li>
        <li><a href="/logout" onclick="confirmLogout()">Log Out</a></li>
      </ul>
    </div>
  </nav>

  <div class="message-container">
    <div class="container">
      <div class="server-message">SERVER MESSAGE WILL SHOW HERE</div>
    </div>
  </div>

  <main class="app">

    <div class="header upper">
      <h2>Upload Image Here To Scan</h2>
      <!-- <div class="server-message"></div> -->

      <!-- esn-start | camera -->

      <!-- <button class="camera-btn" onclick="openCamera()">Open Camera</button>
      <input type="file" accept="image/*" capture="environment" id="mobile-camera-input" style="display: none;" onchange="handleMobileCapture(event)" /> -->

      <!-- <input 
  type="file" 
  accept="image/*" 
  capture="environment" 
  style="display: none;" 
  id="mobileCameraInput" 
  onchange="handleMobileCapture(this)"
>
<button class="camera-btn" onclick="document.getElementById('mobileCameraInput').click()">Open Camera</button>-->

      <input type="file" accept="image/*" capture="environment" style="display: none;" id="mobileCameraInput"
        onchange="handleMobileCapture(this)">
      <button class="camera-btn" onclick="document.getElementById('mobileCameraInput').click()">Open Camera</button>

      <!-- esn-end | camera -->

    </div>

    <div class="input-div">
      <p>Drag and Drop Images or <span class="browse">Browse</span></p>
      <input type="file" class="file" accept="image/png, image/jpeg, image/jpg" />
    </div>

    <!-- esn-start | camera -->

    <div class="camera-div" id="camera-div">
      <div class="container">
        <div class="camera-container" id="camera-container">
          <video class="camera-video" id="camera-video" width="400" height="300" autoplay></video>
          <button class="camera-btn" onclick="captureImage()">Capture Image</button>
          <button class="camera-btn" onclick="closeCamera()">Close Camera</button>
        </div>

        <div class="camera-modal" id="camera-modal">
          <div class="camera-modal-content">
            <img class="camera-preview-image" id="camera-preview-image" src="" alt="Captured Image" />
            <div class="camera-buttons">
              <button class="camera-save-btn" onclick="uploadImage()">Upload</button>
              <button class="camera-cancel-btn" onclick="cancelUpload()">Cancel</button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- esn-end | camera -->

    <div class="image-divs">
      <form id="queued-form">
        <div class="header">
          <h3>Images Preview</h3>
          <button type="submit">Upload</button>
        </div>

        <div class="queued-div"></div>
      </form>

      <form id="saved-form">
        <div class="header lower">
          <h3>Uploaded Image</h3>
          <div class="buttons">
            <button id="scan" type="button" onclick="ScanImage()">Scan</button>
            <!-- <div id="scan">Scan</div> -->

            <button id="delete" type="submit">Delete</button>
          </div>
        </div>

        <div class="saved-div"></div>
      </form>
    </div>
  </main>

  <div id="modal_container" class="modal-container">
    <div class="modal">
      <h1 id="modal-title">Extraction Results</h1>

      <div class="loader active">
        <div class="container">
          <div class="loading">
            <div class="text">Generating medical insight... Please wait.</div>
            <!-- <div class="percent"> 100%</div> -->
            <!-- <div class="progress-bar">
              <div class="progress"></div>
            </div> -->
          </div>
        </div>
      </div>

      <div class="selector"></div>

      <div class="tabs">

        <div class="container">
          <div class="tab-container">
            <div class="tab-box">
              <button class="tab-btn active">Medication Details</button>
              <button class="tab-btn">Drug Interactions</button>
              <button class="tab-btn">Dosage Guidelines</button>
              <button class="tab-btn">Side Effects</button>
              <button class="tab-btn">Drug Prices</button>
              <!-- <div class="line"></div> -->
            </div>
            <div class="content-box">
              <div class="content active" id="drug_information">

              </div>

              <div class="content" id="interactions">

              </div>

              <div class="content" id="indications">

              </div>

              <div class="content" id="side_effects">

              </div>

              <div class="content" id="price">

              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="modal-buttons">
        <div class="modal-buttons2">
        <button type="button" id="save" onclick="saveData();">Save</button>
        <div class="server-message-modal">SERVER MESSAGE WILL SHOW HERE</div>
        </div>
        
        <div class="modal-buttons3">
          
          <button id="back">Back</button>
          <button id="close">Close</button>
        </div>
      </div>
    </div>
  </div>

  <footer class="footer">
    <div class="container">
      <div class="f-1">
        <h2>MedExtract</h2>
        <p>
          Empowering healthcare through intelligent data. Our platform 
          simplifies clinical narratives, making vital insights easier to access,
           understand, and act upon for better patient care outcomes.
        </p>
      </div>

      <div class="f-2">
        <div>
          <h2>Links</h2>
          <ul class="nav">
            <li><a href="/user-extract">Extract</a></li>
            <li><a href="/user-search">Search</a></li>
            <li><a href="/user-settings">Settings</a></li>
            <li><a href="/logout" onclick="confirmLogout()">Log Out</a></li>
          </ul>
        </div>
      </div>

      <div class="f-3">
        <h2>Contacts</h2>
        <div class="contacts">
          <div>
            <img src="/flaticons/user/location.png" /> &nbsp;
            <p>
              University of The East Caloocan, 105 Samson Rd, Caloocan, Metro
              Manila
            </p>
          </div>
          <div>
            <img src="/flaticons/user/email.png" /> &nbsp;
            <p>nogot.markvincent@ue.edu.ph</p>
          </div>
          <div>
            <img src="/flaticons/user/call.png" /> &nbsp;
            <p>09991624156</p>
          </div>
        </div>
      </div>
    </div>
  </footer>

  <script>
    let queuedImagesArray = [];
    savedForm = document.querySelector("#saved-form");
    // deleteBtn = document.querySelector("#delete");
    queuedForm = document.querySelector("#queued-form");
    savedDiv = document.querySelector(".saved-div");
    queuedDiv = document.querySelector(".queued-div");
    inputDiv = document.querySelector(".input-div");
    input = document.querySelector(".input-div input");

    serverMessage = document.querySelector(".server-message");
    serverMessageContainer = document.querySelector(".message-container .container");
    serverMessageModal = document.querySelector(".server-message-modal");

    savedImages = JSON.parse('<%- JSON.stringify(images) %>');

    deleteImages = [];

    function confirmLogout() {
      if (confirm("Are you sure you want to log out?")) {
        window.location.href = "/logout";
      }
    }


    // SAVED IN DATABASE IMAGES



    if (savedImages) {
      displayedSavedImages()
    }

    function displayedSavedImages() {
      let image = savedImages[0]; // Only care about one image

      if (image && image.user_id) {
        savedDiv.innerHTML = `
      <div class="image">
        <img id="prescrip-img" src="https://thesis-webpage-v10-production.up.railway.app/user-image/${image.user_id}" alt="Saved Image">
      </div>`;
      } else {
        savedDiv.innerHTML = ""; // Nothing to show
      }
    }





    // function deleteSavedImage(index) { // new 
    //   deleteImages.push(savedImages[index]);
    //   savedImages.splice(index, 1);
    //   displayedSavedImages();
    // }

    savedForm.addEventListener("submit", (e) => { // new
      e.preventDefault();
      deleteImagesFromServer();
    });

    function deleteImagesFromServer() { // new
      fetch("delete", {
        method: "PUT",
        headers: {
          "Accept": "application/json, text/plain, */*",
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ deleteImages })
      })
        .then(response => {
          if (response.status !== 200) throw Error(response.statusText)
          deleteImages = []
          showServerMessage("Image deleted successfully!");
          location.reload();
        })

        .catch(error => {
          showServerMessage(error.message, false);
        });
    }

    // QUEUED IN FRONTEND IMAGES

    input.addEventListener("change", () => {
      const files = input.files;
      if (files.length) {
        // Only keep the first file
        queuedImagesArray = [files[0]];
      }
      queuedForm.reset();
      displayQueuedImages();
    });

    inputDiv.addEventListener("drop", (e) => {
      e.preventDefault();
      const files = e.dataTransfer.files;

      for (let i = 0; i < files.length; i++) {
        if (!files[i].type.match("image")) continue;

        // Only allow the first image to be added
        queuedImagesArray = [files[i]];
        break;
      }

      displayQueuedImages();
    });


    function displayQueuedImages() {
      let images = "";
      queuedImagesArray.forEach((image, index) => {
        images += `<div class="image">
                   <img src="${URL.createObjectURL(image)}" alt="image">
                   </div>`
      })
      queuedDiv.innerHTML = images;
    }

    queuedForm.addEventListener("submit", (e) => {
      e.preventDefault();
      sendQueuedImagesToServer();
    })

    function sendQueuedImagesToServer() {
      const formData = new FormData(queuedForm);

      queuedImagesArray.forEach((image, index) => {
        formData.append(`file[${index}]`, image)
      })

      fetch("upload", { method: "POST", body: formData })

        .then(response => {
          if (response.status !== 200) throw Error(response.statusText)
          showServerMessage("Image uploaded successfully!");
          location.reload()
        })

        .catch(error => {
          showServerMessage(error.message, false);
        });
    }

    // tabs

    const tabs = document.querySelectorAll(".tab-btn");
    const contents = document.querySelectorAll(".content");

    tabs.forEach((tab, index) => {
      tab.addEventListener("click", (e) => {
        tabs.forEach((tab) => {
          tab.classList.remove("active");
        });
        tab.classList.add("active");
        // var line = document.querySelector(".line");
        // line.style.width = e.target.offsetWidth + "px";
        // line.style.left = e.target.offsetLeft + "px";

        contents.forEach((content) => {
          content.classList.remove("active");
        });
        contents[index].classList.add("active");
      });
    });

    // modal


    const modal_container = document.getElementById("modal_container");
    const close = document.getElementById("close");
    const back = document.getElementById("back");


    const modalTitle = document.querySelector("#modal-title");
    const scanButton = document.getElementById("scan"); // Scan button
    // const open = document.getElementById("open");

    const saveBtn = document.querySelector("#save");


    const tabActive = document.querySelector(".tabs");
    const loaderActive = document.querySelector(".loader");
    const selectorDiv = document.querySelector(".selector");


    const drug_information = document.querySelector("#drug_information");
    const interactions = document.querySelector("#interactions");
    const indications = document.querySelector("#indications");
    const side_effects = document.querySelector("#side_effects");
    const price = document.querySelector("#price");

    let userSelectedMedicine = false;


    let loading;

    const userId = "<%= userId %>";



    // open.addEventListener('click', () => {

    // userSelectedMedicine = false; // reset selection

    // modal_container.classList.add('show');

    // // Reset progress bar and start animation
    // count = 0;
    // per = 0;
    // progress.style.width = "0px"; // Reset progress bar width
    // loading = setInterval(animate, 10); // Start the animation
    // })

    const ScanImage = async () => {
      userSelectedMedicine = false; // reset selection

      modal_container.classList.add('show');

      // Reset progress bar and start animation
      count = 0;
      per = 0;
      // progress.style.width = "0px"; // Reset progress bar width
      loading = setInterval(animate, 250); // Start the animation

      const imageUrl = `https://thesis-webpage-v10-production.up.railway.app/api/image-proxy/${userId}`; // FRONT-END URL
      // const imageUrl = "https://thesis-webpage-v10-production.up.railway.app/api/image-proxy/{user_id}";
      console.log("Image URL:", imageUrl); console.log("User ID:", userId);

      console.log("Image URL:", imageUrl); // Log the image URL
      console.log("User ID:", userId); // Log the user ID

      // fetchExtractedMedicines();

      try {
        const response = await fetch("https://thesis-webpage-v10-production-775f.up.railway.app/api/process-image", {  // API_SERVER
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            // Pass session_id in the Authorization header
          },
          body: JSON.stringify({ image_url: imageUrl, user_id: userId }),
        });

        if (!response.ok) {
          throw new Error("Failed to process the image");
        }

        const data = await response.json();
        console.log("Processed Image Results:", data);




        renderMedicineList(data.medicineArray || []);
      } catch (error) {
        console.error("Error processing the image:", error);
      }
    };


    back.addEventListener('click', () => {
      clearInterval(loading); // Stop the interval
      count = 0;
      per = 0;
      // progress.style.width = "0px";

      loaderActive.classList.remove("active");
      tabActive.classList.remove("active");
      back.classList.remove("active");
      saveBtn.classList.remove("active");

      selectorDiv.classList.add("active");
      modalTitle.textContent = "Extraction Results";
    });

    close.addEventListener('click', () => {

      userSelectedMedicine = false; // reset selection
      modal_container.classList.remove('show');

      // Stop and reset the animation
      clearInterval(loading); // Stop the interval
      count = 0;
      per = 0;
      // progress.style.width = "0px"; // Reset progress bar width

      loaderActive.classList.add("active");
      tabActive.classList.remove("active");
      selectorDiv.classList.remove("active");
      saveBtn.classList.remove("active");
      back.classList.remove("active");
      modalTitle.textContent = "Extraction Results";
    });

    // progress bar

    // var progress = document.querySelector(".progress");
    var text = document.querySelector(".text");

    var count = 0; // Start at 0%
    var per = 0;   // Start at 0px

    // var loading = setInterval(animate, 50);

    function animate() {
      if ((count == 100) & (per == 600)) {
        text.classList.remove("text-blink");
        loaderActive.classList.remove("active");

        // ✅ Only show selector if user hasn't selected yet
        if (!userSelectedMedicine) {
          selectorDiv.classList.add("active");
        }

        text.innerHTML = "";

      } else {
        text.innerHTML = "Scanning Image... Please wait.";
        text.classList.add("text-blink");
        per = per + 6;
        count = count + 1;
        // progress.style.width = per + "px";
      }
    }

    function animate2() {
      if ((count == 100) & (per == 600)) {
        text.classList.remove("text-blink");
        loaderActive.classList.remove("active");
        tabActive.classList.add("active");
        back.classList.add("active");
        saveBtn.classList.add("active");
        text.innerHTML = "";


      } else {
        text.innerHTML = "Generating medical insight... Please wait.";
        text.classList.add("text-blink");
        per = per + 6;
        count = count + 1;
        // progress.style.width = per + "px";
      }
    }


    // selector

    // Initialize with extracted medicine names passed from the backend
    // Initialize with extracted medicine names passed from the backend

    // Placeholder for backend injection
    // Placeholder for backend injection
    // Fetch the extracted drug names from the backend

    // Fetch the extracted medicine names from the FastAPI endpoint
    // Fetch the extracted medicine names from the FastAPI endpoint
    async function fetchExtractedMedicines() {
      try {
        const response = await fetch("https://thesis-webpage-v10-production-c1fa.up.railway.app/process-image", { // GOOGLE_VISION API
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify({ user_id: "<%= userId %>" }), // Pass the user ID dynamically
        });

        if (!response.ok) {
          throw new Error("Failed to fetch extracted medicines");
        }
        const data = await response.json();
        const medicineArray = data.medicineArray || [];
        const rawText = data.raw_text;
        console.log("✅ Raw Text:", rawText);
        sendRawTextToBackend(rawText);
        console.log("✅ Extracted Medicines:", medicineArray);

        // Render the medicine list in the UI
        renderMedicineList(medicineArray);
      } catch (error) {
        console.error("Error fetching extracted medicines:", error);
      }
    }

    // Call the function to fetch and display the medicines
    // fetchExtractedMedicines();

    async function sendRawTextToBackend(rawText) {
      try {
        const response = await fetch("https://thesis-webpage-v10-production-55fa.up.railway.app/process-raw-text", { // Update URL as needed SEARCH -API
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify({ raw_text: rawText }),
        });

        if (!response.ok) {
          throw new Error(`Error: ${response.statusText}`);
        }

        const data = await response.json();
        console.log("Response from backend:", data);

        // Handle the response (e.g., update the UI with drug information)
        if (data.success) {
          console.log("Raw text successfully processed!");
        } else {
          console.log("Failed to process raw text.");
        }
      } catch (error) {
        console.error("Error sending raw text to backend:", error);
        showServerMessage("An error occurred while sending raw text to the backend.");
      }
    }


    function renderMedicineList(medicines) {
      // Clear current list
      const selectorDiv = document.querySelector(".selector");
      selectorDiv.innerHTML = "";

      // Create list
      const ul = document.createElement("ul");
      ul.classList.add("medicine-list");

      medicines.forEach((med) => {
        const li = document.createElement("li");
        li.textContent = med;
        li.classList.add("medicine-item");
        li.onclick = () => {
          openMedicineModal(med);
          selectorDiv.classList.remove("active");

        } // Attach modal logic
        ul.appendChild(li);
      });

      selectorDiv.appendChild(ul);
    }

    // Listen for real-time updates using SSE
    const eventSource = new EventSource("https://thesis-webpage-v10-production-775f.up.railway.app/api/stream-medicines"); // API_SERVER

    eventSource.onmessage = (event) => {
      const medicines = JSON.parse(event.data);
      console.log("Real-time update:", medicines);
      renderMedicineList(medicines);
    };

    eventSource.onerror = (error) => {
      console.error("Error with SSE connection:", error);
    };




    async function openMedicineModal(medicineName) {

      // Stop and reset the animation
      clearInterval(loading); // Stop the interval
      count = 0;
      per = 0;
      // progress.style.width = "0px"; // Reset progress bar width

      // Reset progress bar and start animation
      count = 0;
      per = 0;
      // progress.style.width = "0px"; // Reset progress bar width
      loading = setInterval(animate2, 250); // Start the animation
      loaderActive.classList.add("active");

      selectorDiv.classList.remove("active");
      modalTitle.textContent = `${medicineName} Medical Insights`;



      console.log("Selected Medicine:", medicineName);
      //userSelectedMedicine = true; // User made a selection

      // Fetch drug information from the API
      try {
        const response = await fetch("https://thesis-webpage-v10-production-55fa.up.railway.app/get-drug-info", { // SEARCH API
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify({ drug_name: medicineName }),
        });

        if (!response.ok) {
          throw new Error(`Error: ${response.statusText}`);
        }

        const data = await response.json();

        // Update modal content with the response from the backend
        if (data.error) {
          drug_information.textContent = "";
          interactions.textContent = "";
          indications.textContent = "";
          side_effects.textContent = "";
          price.textContent = "";
        } else {
          drug_information.textContent = data.drug_information || "No details available.";
          interactions.textContent = data.interaction || "No interactions available.";
          indications.textContent = data.indication || "No indications available.";
          side_effects.textContent = data.side_effects || "No side effects available.";
          price.textContent = data.price || "No price information available.";

        
        }

        // Show the modal
        // tabActive.classList.add("active");


      } catch (error) {
        console.error("Failed to fetch drug information:", error);
        showServerMessage("Failed to retrieve drug information. Please try again.");
      }
    }

    const saveButton = document.getElementById("save");

    function saveData() {
      // var modalTitle = document.querySelector("#modal-title");
      // var medicineName = modalTitle.textContent.split(" Medical Insights")[0];
      // var drug_information_value = document.getElementById("drug_information").textContent;
      // var interactions_value = document.getElementById("interactions").textContent;
      // var indications_value = document.getElementById("indications").textContent;
      // var side_effects_value = document.getElementById("side_effects").textContent;
      // var price_value =  document.getElementById("price").textContent;

      const modalTitle = document.querySelector("#modal-title");
      const medicineName = modalTitle.textContent.split(" Medical Insights")[0];
      const details = document.getElementById("drug_information").textContent;
      const interactions = document.getElementById("interactions").textContent;
      const guidelines = document.getElementById("indications").textContent;
      const effects = document.getElementById("side_effects").textContent;
      const prices = document.getElementById("price").textContent;

      const data = { medicineName, details, interactions, guidelines, effects, prices };


      fetch('/user/save-history', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
      })
        .then(response => response.json().then(json => {
          if (!response.ok) throw new Error(json.message);
          // showServerMessageModal(json.message);
          showServerMessageModal("Successful!");
        }))
        .catch(error => {
          console.error('Error:', error);
          showServerMessageModal(error.message, false);
        });
    }



    function showServerMessage(message, isSuccess = true) {
      serverMessage.innerHTML = message;
      serverMessage.style.cssText = isSuccess
        ? "background-color: #D4EDDA; color: #1B5E20;"
        : "background-color: #F8D7DA; color: #B71C1C;";
      serverMessage.classList.add("active");
      serverMessageContainer.classList.add("active");

      setTimeout(() => {
        serverMessage.classList.remove("active");
        serverMessageContainer.classList.remove("active");
      }, 4000);
    }

    function showServerMessageModal(message, isSuccess = true) {
      serverMessageModal.innerHTML = message;
      serverMessageModal.style.cssText = isSuccess
        ? "background-color: #D4EDDA; color: #1B5E20;"
        : "background-color: #F8D7DA; color: #B71C1C;";
      serverMessageModal.classList.add("active");


      setTimeout(() => {
        serverMessageModal.classList.remove("active");
      }, 4000);
    }

    // nav

    const userToggle = document.getElementById('user-menu-toggle');
    const userNav = document.getElementById('user-nav');

    userToggle.addEventListener('click', () => {
      userNav.classList.toggle('show');
    });


    // esn-start | camera

    const isMobile = () => /iPhone|iPad|iPod|Android/i.test(navigator.userAgent);
    let capturedImageBlob = null;

    const openCamera = async () => {
      const cameraDiv = document.getElementById("camera-div");
      const cameraContainer = document.getElementById("camera-container");
      const mobileInput = document.getElementById("mobile-camera-input");

      if (isMobile()) {
        mobileInput.click(); // Open native camera
        return;
      }

      cameraDiv.classList.add("active");
      cameraContainer.classList.remove("hide");

      try {
        const stream = await navigator.mediaDevices.getUserMedia({ video: true });
        const cameraVideo = document.getElementById("camera-video");
        cameraVideo.srcObject = stream;
      } catch (err) {
        showServerMessage("Camera not supported or permission denied.", false)
      }
    };

    const captureImage = () => {
      const video = document.getElementById("camera-video");
      const canvas = document.createElement("canvas");
      canvas.width = video.videoWidth;
      canvas.height = video.videoHeight;
      canvas.getContext("2d").drawImage(video, 0, 0);

      const dataUrl = canvas.toDataURL("image/png");
      showPreview(dataUrl);
    };

    const closeCamera = () => {
      const cameraDiv = document.getElementById("camera-div");
      const cameraModal = document.getElementById("camera-modal");
      const cameraContainer = document.getElementById("camera-container");
      const cameraVideo = document.getElementById("camera-video");

      // Stop the camera stream if active
      const stream = cameraVideo.srcObject;
      if (stream) {
        stream.getTracks().forEach(track => track.stop());
        cameraVideo.srcObject = null;
      }

      // Hide everything
      cameraDiv.classList.remove("active");
      cameraModal.classList.remove("active");
      cameraContainer.classList.add("hide");


    };

    const handleMobileCapture = async (input) => {
  const file = input.files[0];
  if (!file) return;

  const reader = new FileReader();
  reader.onload = async () => {
    const base64Image = reader.result;

    try {
      const response = await fetch("/save-image", {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ image: base64Image })
      });

      const result = await response.json();
      if (response.ok) {
        showServerMessage("Image saved successfully.");
        setTimeout(() => location.reload(), 1000); // 🔁 Reload after 1 second
      } else {
        showServerMessage("Failed to save image.", false);
      }
    } catch (err) {
      console.error("Upload error:", err);
      showServerMessage("An error occurred while uploading the image.", false);
    }
  };

  reader.readAsDataURL(file);
};


    const showPreview = (dataUrl) => {
      const cameraDiv = document.getElementById("camera-div");
      const cameraContainer = document.getElementById("camera-container");
      const cameraModal = document.getElementById("camera-modal");
      const previewImage = document.getElementById("camera-preview-image");

      previewImage.src = dataUrl;
      cameraContainer.classList.add("hide");
      cameraModal.classList.add("active");

      capturedImageBlob = dataUrl;
    };

    const cancelUpload = () => {
      const cameraDiv = document.getElementById("camera-div");
      const cameraModal = document.getElementById("camera-modal");
      const cameraContainer = document.getElementById("camera-container");
      const cameraVideo = document.getElementById("camera-video");

      // Always hide the preview modal
      cameraModal.classList.remove("active");

      if (isMobile()) {
        // On mobile, just close completely
        closeCamera();
      } else {
        // On desktop, reopen the camera container and resume stream
        cameraContainer.classList.remove("hide");

        // Restart camera if it was stopped
        navigator.mediaDevices.getUserMedia({ video: true })
          .then(stream => {
            cameraVideo.srcObject = stream;
          })
          .catch(() => {
            showServerMessage("Unable to access camera.", false);
            closeCamera();
          });
      }
    };

    const uploadImage = async () => {
  if (!capturedImageBlob) return showServerMessage("No image to upload.", false);

  try {
    const res = await fetch("/save-image", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ image: capturedImageBlob })
    });

    const result = await res.json();

    if (res.ok) {
      showServerMessage(result.message);
      setTimeout(() => location.reload(), 1000); // 🔁 Reload after 1 second
    } else {
      showServerMessage("Failed to save image.", false);
    }
  } catch (err) {
    console.error("Upload error:", err);
    showServerMessage("Failed to upload image.", false);
  }
};

    // esn-end | camera







  </script>

</body>



</html>